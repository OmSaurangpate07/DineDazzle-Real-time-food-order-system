<section class="hero">
    <div class="container mx-auto flex flex-col items-center justify-center">
        <div class="flex justify-center items-start">
            <img style="width: 55%;" src="https://illustrations.popsy.co/amber/romantic-dinner.svg" alt="">
        </div>
        <div class="flex items-center justify-center flex-col">
            <h6 class="text-2xl md:text-3xl"><em>Are you hungry?</em><span class="text-3xl md:text-4xl font-bold">&nbsp;&nbsp;Don't wait !</span></h6>
            <a href="#menu"><button class="px-6 py-2 rounded-full text-white font-bold mt-4 btn-primary">Order Now</button></a>
        </div>
    </div>
</section>
<section class="menu container mx-auto py-8">
    <h1 class="text-2xl md:text-2xl font-bold mb-8" id="menu">Menu</h1>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 col-gap-12 row-gap-16">
        <% pizzas.forEach(function(pizza) { %>
        <div class="w-full md:w-64">
            <img class="h-40 mb-4 mx-auto" src="/img/<%= pizza.image %>" alt="">
            <div class="text-center">
                <h2 class="mb-4 text-lg"><%= pizza.name %></h2>
                <div class="flex items-center justify-around mt-6">
                    <span class="font-bold text-lg">₹<%= pizza.price %></span>
                    <button data-pizza="<%= JSON.stringify(pizza) %>"
                        class="add-to-cart py-1 px-6 rounded-full flex items-center font-bold">
                        <span>+</span>
                        <span class="ml-4">Add</span>
                    </button>
                </div>
            </div>
        </div>
        <% }) %>

    </div>
</section>

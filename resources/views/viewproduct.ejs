<section class="menu container mx-auto py-8">
    <div class="flex items-center justify-center">
        <a href="/admin/orders"><button
          type="button"
          class="rounded-md mx-4 bg-yellow-600 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-green-600/80 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-green-600"
        >
          Dashboard
        </button></a>
      </div>
</section>
<section class="menu container mx-auto py-8">
  <h1 class="text-2xl md:text-2xl font-bold mb-8" id="menu">Product Menu</h1>
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 col-gap-12 row-gap-16">
      <% pizzas.forEach(function(pizza) { %>
      <div class="w-full md:w-64">
          <img class="h-40 mb-4 mx-auto" src="/img/<%= pizza.image %>" alt="">
          <div class="text-center flex justify-around">
              <h2 class="mb-4 text-lg"><%= pizza.name %></h2>
              <!-- <span class="size py-1 px-4 rounded-full uppercase text-xs"><%= pizza.size %></span> -->
              <!-- <div class="flex items-center justify-around mt-6"> -->
                  <span class="font-bold text-lg">₹<%= pizza.price %></span>
              <!-- </div> -->
          </div>
      </div>
      <% }) %>

  </div>
</section>